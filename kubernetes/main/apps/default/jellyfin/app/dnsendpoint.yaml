---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: ${APP}-internal
  annotations:
    external-dns/private: "true"
spec:
  endpoints:
    - dnsName: ${APP}.lumu.au
      recordTTL: 300
      recordType: A
      targets:
        - 10.88.0.21
    - dnsName: ${APP}.lumu.au
      recordTTL: 300
      recordType: AAAA
      targets:
        - 2404:e80:942a:88::21
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: ${APP}-external
  annotations:
    external-dns/public: "true"
spec:
  endpoints:
    - dnsName: ${APP}.lumu.au
      recordTTL: 300
      recordType: A
      targets:
        - ${EXTERNAL_IP}
    - dnsName: ${APP}.lumu.au
      recordTTL: 300
      recordType: AAAA
      targets:
        - ${EXTERNAL_IP_AAAA}
