---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: headlamp
spec:
  chartRef:
    kind: OCIRepository
    name: headlamp
  interval: 30m
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    replicaCount: 1
    config:
      baseURL: ""
      pluginsDir: /headlamp/plugins
    initContainers:
      - name: create-kubeconfig
        image: rancher/shell:v7.0.0@sha256:6083fb888fec64342d7a5b8df164a092ea9a92cd2513151dd9fa1766c15dc497
        command:
          - /bin/sh
          - -c
          - |
            cat <<EOF > /config/kubeconfig
            apiVersion: v1
            kind: Config
            clusters:
            - cluster:
                certificate-authority: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
                server: https://kubernetes.default.svc
              name: default
            contexts:
            - context:
                cluster: default
                user: default
                namespace: default
              name: default
            current-context: default
            users:
            - name: default
              user:
                tokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
            EOF
        volumeMounts:
          - mountPath: /config
            name: kubeconfig
      - name: headlamp-plugins
        image: ghcr.io/headlamp-k8s/headlamp-plugin-flux:v0.6.0@sha256:718f987c7ac73214d60fd3bcf81ef5db494df5a2746a1ed990b40c6eff2596bf
        command:
          - /bin/sh
          - -c
          - |
            cp -r /plugins/* /headlamp/plugins/
        volumeMounts:
          - mountPath: /headlamp/plugins
            name: plugins
    env:
      - name: KUBECONFIG
        value: /config/kubeconfig
    extraArgs:
      - -plugins-dir=/headlamp/plugins
    volumeMounts:
      - mountPath: /headlamp/plugins
        name: plugins
      - mountPath: /config
        name: kubeconfig
      - mountPath: /var/run/secrets/kubernetes.io/serviceaccount
        name: headlamp-admin-token
        readOnly: true
    volumes:
      - name: plugins
        emptyDir: {}
      - name: kubeconfig
        emptyDir: {}
      - name: headlamp-admin-token
        secret:
          secretName: headlamp-admin-token
    service:
      port: 4466
    serviceAccount:
      create: false
      name: headlamp-admin
    podAnnotations:
      reloader.stakater.com/auto: "true"
    securityContext:
      runAsUser: 100
      runAsGroup: 101
